# Public Assets

This directory contains static assets for PennyPlan:

- `models/` — Lightweight CC0 .glb proxy models for 3D visualization
  - In the prototype, colored boxes are used as proxies scaled to exact furniture dimensions
  - In production, place generic CC0 furniture .glb files here for more realistic previews

Place your 3D models in the `models/` subdirectory.
